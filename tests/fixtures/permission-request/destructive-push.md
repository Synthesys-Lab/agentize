# Test Fixture: destructive-push.json

## Purpose
Mock permission request for testing destructive git push operations (force push) in the Claude Code permission hook system.

## Expected Behavior
Permission hook should block this request as destructive operations require manual user approval.
